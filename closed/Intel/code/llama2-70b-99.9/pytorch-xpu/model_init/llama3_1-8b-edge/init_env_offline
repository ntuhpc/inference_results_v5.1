#!/bin/bash

source $MODEL_INIT_DIR/init_env_shared
# Validate and set Tensor parallel and Instances
export TP=1
export NUM_INSTS=1

if (( XPU_COUNT > 0 )); then
    # XPU
    export TP=1
    export NUM_INSTS=$((XPU_COUNT / TP))
    export BATCH_SIZE=128
else
    echo "Error: No XPU found. CPU is not enabled for this workload"
fi
